# Runtime-only Dockerfile that uses the already-built JAR
FROM docker.io/library/openjdk:21-jre
WORKDIR /app
COPY target/clinic-0.0.1-SNAPSHOT.jar app.jar
EXPOSE 8082
ENTRYPOINT ["java","-jar","/app/app.jar"]
